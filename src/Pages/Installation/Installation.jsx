import React from 'react';
import Error from '../Error/Error';
import AppError from '../Error/AppError';

const Installation = () => {
    return (
        <div className='hero-font'>
          <h1>installation page is here....</h1>
          <AppError></AppError>
        </div>
    );
};

export default Installation;